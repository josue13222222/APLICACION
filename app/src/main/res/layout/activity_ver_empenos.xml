<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/tech_background">

    <!-- Encabezado mejorado -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/btn_rounded"
        android:padding="16dp"
        android:layout_marginBottom="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="💎 Mis Empeños"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginBottom="12dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Busca tu número de teléfono para ver el estado de tus empeños"
            android:textSize="13sp"
            android:textColor="#CCCCCC"
            android:layout_marginBottom="12dp" />

        <!-- Campo de búsqueda mejorado -->
        <EditText
            android:id="@+id/etTelefonoUsuario"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="📞 Ej: 987654321"
            android:inputType="phone"
            android:padding="12dp"
            android:background="@android:color/white"
            android:textColor="@android:color/black"
            android:textColorHint="@android:color/darker_gray"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btnBuscarEmpenos"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="🔍 Buscar Mis Empeños"
            android:textAllCaps="false"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/btn_rounded"
            android:textColor="@android:color/white" />

    </LinearLayout>

    <!-- Lista de empeños con mejor diseño -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEmpenos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="0dp"
        android:paddingEnd="0dp" />

</LinearLayout>
